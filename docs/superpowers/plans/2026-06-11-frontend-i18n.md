# Frontend i18n Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add Russian + English localization to `frontend/app`, defaulting to Russian, with a language switcher on the marketing landing and in the app sidebar; translate the whole app.

**Architecture:** `react-i18next` initialized from a single `src/i18n` module with statically-bundled, per-feature JSON namespaces. Language is detected from `localStorage` (`phenograph.lang`) and falls back to `ru`. Components consume strings via `useTranslation(ns)` / `t(key)`. A key-parity test guards RU/EN completeness; a TS module augmentation gives compile-time key checking.

**Tech Stack:** React 19, TypeScript 6, Vite 8, `i18next` + `react-i18next` + `i18next-browser-languagedetector`, Vitest + @testing-library/react (new).

---

## File Structure

**New files**
- `src/i18n/index.ts` — i18next init; exports the configured `i18n` instance.
- `src/i18n/resources.ts` — imports every locale JSON and assembles the `resources` + `defaultNS` + `ns` constants. Single source of truth shared by init and tests.
- `src/i18n/react-i18next.d.ts` — module augmentation typing `t()` keys from the RU resources.
- `src/i18n/HtmlLangSync.tsx` — tiny component syncing `<html lang>` to the active language.
- `src/i18n/locales/{ru,en}/{common,nav,marketing,auth,cases,labs,reference,notifications,profile}.json` — translation dictionaries (18 files).
- `src/i18n/i18n.test.ts` — config + key-parity tests.
- `src/components/ui/LanguageSwitcher.tsx` — RU/EN dropdown.
- `src/components/ui/LanguageSwitcher.test.tsx` — switcher behavior test.
- `src/test/setup.ts` — Vitest setup (jest-dom matchers).
- `src/features/marketing/LandingPage.test.tsx` — bilingual smoke render.

**Modified files**
- `package.json` — deps + `test` script.
- `vite.config.ts` — Vitest config block.
- `tsconfig.app.json` — `resolveJsonModule: true`.
- `src/main.tsx` — import `./i18n` before render; mount `<HtmlLangSync />`.
- `src/components/layout/nav.ts` — add stable `key` to each `NavItem`; labels become i18n keys.
- `src/components/layout/Sidebar.tsx` — resolve nav labels + notifications badge via `key`.
- `src/components/layout/AppShell.tsx` — translate topbar (primary button, aria-labels).
- `src/components/ui/{feedback,form,Pagination,Dropzone}.tsx` — translate built-in defaults.
- `src/features/marketing/{sections,ScrollStory}.tsx` — translate marketing copy + add switcher.
- `src/features/auth/{AuthLayout,LoginPage,RegisterPage}.tsx` — translate auth copy/errors.
- `src/features/cases/{CasesListPage,NewCasePage,CaseDetailPage}.tsx` — translate.
- `src/features/labs/{BatchesPage,NewBatchPage,BatchDetailPage,UsagePage,BillingPage}.tsx` — translate.
- `src/features/reference/{SyndromesPage,SyndromeDetailPage,HpoAtlasPage}.tsx` — translate.
- `src/features/notifications/NotificationsPage.tsx`, `src/features/profile/ProfilePage.tsx`, `src/features/NotFoundPage.tsx` — translate.

---

## Translation Glossary (use consistently across all RU files)

| English | Russian |
|---|---|
| Phenograph (brand) | Phenograph (do not translate) |
| syndrome / syndromes | синдром / синдромы |
| phenotype / phenotyping | фенотип / фенотипирование |
| morphological feature(s) | морфологический признак / признаки |
| clinician / doctor | врач |
| laboratory / lab | лаборатория |
| case / cases | случай / случаи |
| batch / batches | пакет / пакеты |
| diagnosis / diagnoses | диагноз / диагнозы |
| confidence | достоверность |
| upload (verb / noun) | загрузить / загрузка |
| Sign in / Sign out | Войти / Выйти |
| Request access | Запросить доступ |
| Create account | Создать аккаунт |
| Notifications | Уведомления |
| Profile | Профиль |
| Usage | Использование |
| Billing | Оплата |
| Feature atlas | Атлас признаков |
| New analysis / New case / New batch | Новый анализ / Новый случай / Новый пакет |
| Account type | Тип аккаунта |
| Email / Password / Full name / Organization | Email / Пароль / Полное имя / Организация |
| Pending / Processing / Completed / Failed | Ожидание / Обработка / Завершено / Ошибка |
| Loading… | Загрузка… |
| Retry | Повторить |
| Page not found | Страница не найдена |
| Something went wrong | Что-то пошло не так |
| Prev / Next | Назад / Вперёд |
| Page {{n}} | Страница {{n}} |

Proper nouns kept verbatim in **both** locales: model/version IDs (`pg-face-3.2`, `HPO v2024.4`, `pg · 01`), publication venues (Nature Genetics, AJHG, JAMA Pediatrics), standards (HIPAA, SOC 2 Type II, GDPR), brand "Phenograph".

---

## Task 1: Install dependencies

**Files:** `package.json`, `package-lock.json`

- [ ] **Step 1: Install runtime + dev deps**

```bash
cd frontend/app
npm install i18next react-i18next i18next-browser-languagedetector
npm install -D vitest jsdom @testing-library/react @testing-library/jest-dom @testing-library/user-event @testing-library/dom
```

If peer-dependency warnings appear against the repo's Vite 8 / React 19, accept the latest published majors — do not pin older versions.

- [ ] **Step 2: Add the test script**

In `package.json` `"scripts"`, add (keep existing scripts):

```json
"test": "vitest run",
"test:watch": "vitest"
```

- [ ] **Step 3: Verify install**

Run: `npm ls i18next react-i18next vitest`
Expected: all three resolve with versions, no `UNMET` lines.

- [ ] **Step 4: Commit**

```bash
git add package.json package-lock.json
git commit -m "chore: add i18next + vitest dependencies"
```

---

## Task 2: Configure Vitest

**Files:** Modify `vite.config.ts`; Create `src/test/setup.ts`

- [ ] **Step 1: Create the test setup file**

`src/test/setup.ts`:

```ts
import '@testing-library/jest-dom/vitest';
```

- [ ] **Step 2: Add Vitest config to vite.config.ts**

Replace `vite.config.ts` with:

```ts
/// <reference types="vitest/config" />
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./src/test/setup.ts'],
  },
})
```

- [ ] **Step 3: Write a throwaway smoke test to prove the runner works**

`src/test/setup.test.ts`:

```ts
import { describe, expect, it } from 'vitest';

describe('vitest runner', () => {
  it('runs', () => {
    expect(1 + 1).toBe(2);
  });
});
```

- [ ] **Step 4: Run it**

Run: `npm test`
Expected: PASS (1 test). If jsdom/globals errors appear, fix config before continuing.

- [ ] **Step 5: Delete the throwaway test and commit**

```bash
rm src/test/setup.test.ts
git add vite.config.ts src/test/setup.ts
git commit -m "test: configure vitest with jsdom + testing-library"
```

---

## Task 3: Enable JSON imports

**Files:** Modify `tsconfig.app.json`

- [ ] **Step 1: Add resolveJsonModule**

In `tsconfig.app.json` `compilerOptions`, add after `"skipLibCheck": true,`:

```json
"resolveJsonModule": true,
```

- [ ] **Step 2: Commit**

```bash
git add tsconfig.app.json
git commit -m "chore: enable resolveJsonModule for locale imports"
```

---

## Task 4: Create the `common` and `nav` namespaces

**Files:** Create `src/i18n/locales/{ru,en}/common.json`, `src/i18n/locales/{ru,en}/nav.json`

- [ ] **Step 1: `src/i18n/locales/en/common.json`**

```json
{
  "actions": {
    "retry": "Retry",
    "backToHome": "Back to home",
    "signIn": "Sign in",
    "signOut": "Sign out",
    "requestAccess": "Request access",
    "createAccount": "Create account",
    "markAsRead": "Mark as read"
  },
  "state": {
    "loading": "Loading…",
    "somethingWentWrong": "Something went wrong",
    "empty": "Nothing here yet"
  },
  "status": {
    "pending": "Pending",
    "processing": "Processing",
    "completed": "Completed",
    "failed": "Failed"
  },
  "pagination": {
    "page": "Page {{page}}",
    "prev": "Prev",
    "next": "Next"
  },
  "dropzone": {
    "promptOne": "Drop an image here, or click to browse",
    "promptMany": "Drop images here, or click to browse"
  },
  "lang": {
    "label": "Language",
    "ru": "Русский",
    "en": "English"
  },
  "value": {
    "none": "—",
    "ageYears": "{{age}}y"
  }
}
```

- [ ] **Step 2: `src/i18n/locales/ru/common.json`**

```json
{
  "actions": {
    "retry": "Повторить",
    "backToHome": "На главную",
    "signIn": "Войти",
    "signOut": "Выйти",
    "requestAccess": "Запросить доступ",
    "createAccount": "Создать аккаунт",
    "markAsRead": "Отметить как прочитанное"
  },
  "state": {
    "loading": "Загрузка…",
    "somethingWentWrong": "Что-то пошло не так",
    "empty": "Здесь пока пусто"
  },
  "status": {
    "pending": "Ожидание",
    "processing": "Обработка",
    "completed": "Завершено",
    "failed": "Ошибка"
  },
  "pagination": {
    "page": "Страница {{page}}",
    "prev": "Назад",
    "next": "Вперёд"
  },
  "dropzone": {
    "promptOne": "Перетащите изображение сюда или нажмите для выбора",
    "promptMany": "Перетащите изображения сюда или нажмите для выбора"
  },
  "lang": {
    "label": "Язык",
    "ru": "Русский",
    "en": "English"
  },
  "value": {
    "none": "—",
    "ageYears": "{{age}} г."
  }
}
```

- [ ] **Step 3: `src/i18n/locales/en/nav.json`**

```json
{
  "cases": "Cases",
  "newAnalysis": "New analysis",
  "syndromes": "Syndromes",
  "atlas": "Feature atlas",
  "notifications": "Notifications",
  "profile": "Profile",
  "batches": "Batches",
  "newBatch": "New batch",
  "usage": "Usage",
  "billing": "Billing"
}
```

- [ ] **Step 4: `src/i18n/locales/ru/nav.json`**

```json
{
  "cases": "Случаи",
  "newAnalysis": "Новый анализ",
  "syndromes": "Синдромы",
  "atlas": "Атлас признаков",
  "notifications": "Уведомления",
  "profile": "Профиль",
  "batches": "Пакеты",
  "newBatch": "Новый пакет",
  "usage": "Использование",
  "billing": "Оплата"
}
```

- [ ] **Step 5: Commit**

```bash
git add src/i18n/locales
git commit -m "feat(i18n): add common + nav locale dictionaries"
```

---

## Task 5: Assemble resources + initialize i18next

**Files:** Create `src/i18n/resources.ts`, `src/i18n/index.ts`

At this point only `common` and `nav` exist. Later namespace tasks (8–15) each append their JSON imports to `resources.ts` following the exact same pattern shown here. Keep the `NAMESPACES` array and the per-locale objects in sync whenever a namespace is added.

- [ ] **Step 1: `src/i18n/resources.ts`**

```ts
import enCommon from './locales/en/common.json';
import enNav from './locales/en/nav.json';
import ruCommon from './locales/ru/common.json';
import ruNav from './locales/ru/nav.json';

export const NAMESPACES = ['common', 'nav'] as const;
export type Namespace = (typeof NAMESPACES)[number];

export const defaultNS = 'common';

export const resources = {
  en: { common: enCommon, nav: enNav },
  ru: { common: ruCommon, nav: ruNav },
} as const;

export type AppResources = (typeof resources)['ru'];
```

- [ ] **Step 2: `src/i18n/index.ts`**

```ts
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import LanguageDetector from 'i18next-browser-languagedetector';
import { defaultNS, resources } from './resources';

export const SUPPORTED_LANGUAGES = ['ru', 'en'] as const;
export type AppLanguage = (typeof SUPPORTED_LANGUAGES)[number];

i18n
  .use(LanguageDetector)
  .use(initReactI18next)
  .init({
    resources,
    defaultNS,
    fallbackLng: 'ru',
    supportedLngs: [...SUPPORTED_LANGUAGES],
    nonExplicitSupportedLngs: true,
    detection: {
      order: ['localStorage'],
      caches: ['localStorage'],
      lookupLocalStorage: 'phenograph.lang',
    },
    interpolation: { escapeValue: false },
  });

export default i18n;
```

- [ ] **Step 3: Commit**

```bash
git add src/i18n/resources.ts src/i18n/index.ts
git commit -m "feat(i18n): initialize i18next with localStorage detection, ru fallback"
```

---

## Task 6: Key-parity + config tests (TDD)

**Files:** Create `src/i18n/i18n.test.ts`

- [ ] **Step 1: Write the tests**

`src/i18n/i18n.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import i18n, { SUPPORTED_LANGUAGES } from './index';
import { NAMESPACES, resources } from './resources';

/** Recursively collect dotted key paths from a nested dictionary. */
function keyPaths(obj: unknown, prefix = ''): string[] {
  if (obj === null || typeof obj !== 'object') return [prefix];
  return Object.entries(obj as Record<string, unknown>).flatMap(([k, v]) =>
    keyPaths(v, prefix ? `${prefix}.${k}` : k),
  );
}

describe('i18n config', () => {
  it('defaults to Russian', () => {
    expect(i18n.language).toBe('ru');
    expect(i18n.options.fallbackLng).toContain('ru');
  });

  it('persists the chosen language to localStorage', async () => {
    await i18n.changeLanguage('en');
    expect(localStorage.getItem('phenograph.lang')).toBe('en');
    await i18n.changeLanguage('ru');
  });
});

describe('locale key parity', () => {
  it.each(NAMESPACES)('ru and en have identical keys in "%s"', (ns) => {
    const ru = keyPaths(resources.ru[ns]).sort();
    const en = keyPaths(resources.en[ns]).sort();
    expect(ru).toEqual(en);
  });

  it('covers every supported language', () => {
    for (const lng of SUPPORTED_LANGUAGES) {
      expect(resources[lng]).toBeDefined();
    }
  });
});
```

- [ ] **Step 2: Run tests**

Run: `npm test`
Expected: PASS (all parity + config tests green with the `common`/`nav` namespaces).

- [ ] **Step 3: Commit**

```bash
git add src/i18n/i18n.test.ts
git commit -m "test(i18n): key parity + default-language + persistence tests"
```

> From here on, every namespace task ends by re-running `npm test`; the parity test automatically covers new namespaces once they are added to `NAMESPACES` and `resources`.

---

## Task 7: Type augmentation

**Files:** Create `src/i18n/react-i18next.d.ts`

- [ ] **Step 1: Write the augmentation**

`src/i18n/react-i18next.d.ts`:

```ts
import 'react-i18next';
import type { AppResources } from './resources';

declare module 'react-i18next' {
  interface CustomTypeOptions {
    defaultNS: 'common';
    resources: AppResources;
  }
}
```

- [ ] **Step 2: Type-check**

Run: `npx tsc -b`
Expected: no errors.

- [ ] **Step 3: Commit**

```bash
git add src/i18n/react-i18next.d.ts
git commit -m "feat(i18n): type-safe translation keys via module augmentation"
```

---

## Task 8: LanguageSwitcher component (TDD)

**Files:** Create `src/components/ui/LanguageSwitcher.tsx`, `src/components/ui/LanguageSwitcher.test.tsx`; Modify `src/components/ui/index.ts`

- [ ] **Step 1: Write the failing test**

`src/components/ui/LanguageSwitcher.test.tsx`:

```tsx
import { describe, expect, it, beforeEach } from 'vitest';
import { render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import i18n from '../../i18n';
import { LanguageSwitcher } from './LanguageSwitcher';

describe('LanguageSwitcher', () => {
  beforeEach(async () => {
    await i18n.changeLanguage('ru');
  });

  it('reflects the current language and switches it', async () => {
    render(<LanguageSwitcher />);
    const select = screen.getByRole('combobox', { name: /язык|language/i });
    expect((select as HTMLSelectElement).value).toBe('ru');

    await userEvent.selectOptions(select, 'en');
    expect(i18n.language).toBe('en');
    expect((select as HTMLSelectElement).value).toBe('en');
  });
});
```

- [ ] **Step 2: Run it — confirm failure**

Run: `npm test -- LanguageSwitcher`
Expected: FAIL (module not found / no component).

- [ ] **Step 3: Implement the component**

`src/components/ui/LanguageSwitcher.tsx`:

```tsx
import { Languages } from 'lucide-react';
import { useTranslation } from 'react-i18next';
import { SUPPORTED_LANGUAGES } from '../../i18n';

export function LanguageSwitcher({ compact = false }: { compact?: boolean }) {
  const { t, i18n } = useTranslation('common');
  return (
    <label style={{ display: 'inline-flex', alignItems: 'center', gap: 6, color: 'var(--ink-2)' }}>
      <Languages size={compact ? 14 : 16} strokeWidth={1.75} aria-hidden />
      <span style={{ position: 'absolute', width: 1, height: 1, overflow: 'hidden', clip: 'rect(0 0 0 0)' }}>
        {t('lang.label')}
      </span>
      <select
        aria-label={t('lang.label')}
        value={i18n.resolvedLanguage}
        onChange={(e) => i18n.changeLanguage(e.target.value)}
        style={{
          appearance: 'none', background: 'transparent', border: 'none',
          color: 'inherit', fontFamily: 'var(--font-body)', fontSize: 'var(--fs-14)',
          fontWeight: 500, cursor: 'pointer', padding: '2px 4px',
        }}
      >
        {SUPPORTED_LANGUAGES.map((lng) => (
          <option key={lng} value={lng}>{t(`lang.${lng}`)}</option>
        ))}
      </select>
    </label>
  );
}
```

- [ ] **Step 4: Export from the UI barrel**

In `src/components/ui/index.ts` add:

```ts
export { LanguageSwitcher } from './LanguageSwitcher';
```

- [ ] **Step 5: Run the test — confirm pass**

Run: `npm test -- LanguageSwitcher`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add src/components/ui/LanguageSwitcher.tsx src/components/ui/LanguageSwitcher.test.tsx src/components/ui/index.ts
git commit -m "feat(i18n): add LanguageSwitcher component"
```

---

## Task 9: Wire i18n into the app entry + html lang sync

**Files:** Create `src/i18n/HtmlLangSync.tsx`; Modify `src/main.tsx`

- [ ] **Step 1: Create HtmlLangSync**

`src/i18n/HtmlLangSync.tsx`:

```tsx
import { useEffect } from 'react';
import { useTranslation } from 'react-i18next';

/** Keeps <html lang> in sync with the active language. Renders nothing. */
export function HtmlLangSync() {
  const { i18n } = useTranslation();
  useEffect(() => {
    document.documentElement.lang = i18n.resolvedLanguage ?? 'ru';
  }, [i18n.resolvedLanguage]);
  return null;
}
```

- [ ] **Step 2: Import i18n + mount HtmlLangSync in main.tsx**

In `src/main.tsx`, add the i18n import alongside the CSS imports (it must run before render):

```ts
import './i18n';
import { HtmlLangSync } from './i18n/HtmlLangSync';
```

Then add `<HtmlLangSync />` as the first child inside `<BrowserRouter>`:

```tsx
        <BrowserRouter>
          <HtmlLangSync />
          <AuthProvider>
            <ToastProvider>
              <App />
            </ToastProvider>
          </AuthProvider>
        </BrowserRouter>
```

- [ ] **Step 3: Verify dev server boots in Russian**

Run: `npm run dev` (then open http://localhost:5173, then stop)
Expected: landing page renders; `<html lang="ru">` in devtools. (Marketing text still English until Task 10 — that's fine.)

- [ ] **Step 4: Commit**

```bash
git add src/main.tsx src/i18n/HtmlLangSync.tsx
git commit -m "feat(i18n): bootstrap i18n at app entry + sync html lang"
```

---

## Task 10: Translate the marketing landing + add switcher

**Files:** Create `src/i18n/locales/{ru,en}/marketing.json`; Modify `src/i18n/resources.ts`, `src/features/marketing/sections.tsx`, `src/features/marketing/ScrollStory.tsx`

- [ ] **Step 1: `src/i18n/locales/en/marketing.json`**

```json
{
  "nav": {
    "product": "Product",
    "evidence": "Evidence",
    "forClinicians": "For clinicians",
    "research": "Research"
  },
  "story": {
    "act1Eyebrow": "Computational dysmorphology",
    "act1Tag": "PG · 01",
    "title": "Phenotype, precisely read.",
    "lede": "Phenograph identifies morphological features from a single facial image and ranks candidate syndromes against the HPO ontology.",
    "ctaPrimary": "Request clinical access",
    "ctaSecondary": "Read the evidence",
    "figIdle": "FIG. 01 — IDLE STATE",
    "figLine1": "Double-strand reference pattern.",
    "figLine2": "127 morphological features indexed.",
    "figLine3": "4,317 syndromes linked to HPO.",
    "scrollToContinue": "Scroll to continue",
    "scroll": "Scroll",
    "act2Tag": "PG · 02 / THE MESH",
    "act2Title": "From a strand of code to a face in reference.",
    "act2Body": "Sub-millimeter landmarks are localized against a curated atlas of 127 morphological features, then triangulated into a reference mesh for metric analysis.",
    "meshStage": "STAGE",
    "meshLandmarks": "Landmarks",
    "meshEdges": "Edges",
    "meshAtlas": "Atlas",
    "meshModel": "Model",
    "meshDisclaimer": "All imagery shown is an abstract reference mesh. Phenograph never exposes identifiable patient faces.",
    "stages": {
      "arrival": "01 · ARRIVAL",
      "landmarking": "02 · LANDMARKING",
      "triangulation": "03 · TRIANGULATION",
      "contour": "04 · CONTOUR",
      "annotated": "05 · ANNOTATED"
    },
    "act3Tag": "PG · 03 / THE RECORD",
    "act3Title": "Validated evidence, quietly compounding.",
    "asOf": "As of Apr 2026",
    "liveDataset": "● LIVE DATASET",
    "disclaimerShort": "Not a diagnostic device. Clinical decision support only.",
    "publications": "Publications",
    "validationReport": "Validation report",
    "stat": {
      "accuracyLabel": "Top-5 accuracy on blind validation",
      "accuracySub": "n = 12,400 genetically confirmed cases",
      "accuracyChart": "2018 — 2025",
      "syndromesLabel": "Syndromes linked in knowledge graph",
      "syndromesSub": "HPO-aligned · updated quarterly",
      "syndromesChart": "6 releases",
      "featuresLabel": "Morphological features scored",
      "featuresSub": "Each with calibrated uncertainty",
      "featuresChart": "Atlas growth",
      "pubsLabel": "Peer-reviewed publications",
      "pubsSub": "Nature Genetics · AJHG · JAMA Pediatrics",
      "pubsChart": "by year"
    },
    "rail": { "read": "The read", "mesh": "The mesh", "record": "The record" }
  },
  "features": {
    "eyebrow": "What it does",
    "heading": "A quiet tool that extends clinical expertise — not a replacement for it.",
    "landmark": { "title": "Landmark phenotyping", "body": "Sub-millimeter facial landmarking against a curated atlas of 127 morphological features, scored with uncertainty." },
    "ranking": { "title": "Syndrome ranking", "body": "Bayesian ranking across 4,300+ syndromes linked to the Human Phenotype Ontology. Clinicians see posterior probabilities, not black-box scores." },
    "workflow": { "title": "Clinical workflow", "body": "Case notes, features, and candidate panels export to FHIR. Integrates with existing EHRs without forcing a rebuild." }
  },
  "evidence": {
    "eyebrow": "Evidence",
    "heading": "Validated against 12,000+ genetically confirmed cases across 41 institutions.",
    "accuracy": "Top-5 accuracy on blind validation set of rare syndrome cases.",
    "syndromes": "Syndromes covered in the linked knowledge graph. HPO-aligned.",
    "publications": "Peer-reviewed publications. Nature Genetics, AJHG, JAMA Pediatrics."
  },
  "footer": {
    "tagline": "Computational dysmorphology for clinical genetics.",
    "copyright": "© 2026 Phenograph, Inc. Not a diagnostic device. For clinical decision support only.",
    "cols": {
      "product": "Product",
      "evidence": "Evidence",
      "company": "Company",
      "legal": "Legal"
    },
    "links": {
      "overview": "Overview", "clinicalWorkflow": "Clinical workflow", "integrations": "Integrations", "security": "Security", "changelog": "Changelog",
      "publications": "Publications", "validation": "Validation", "hpoAlignment": "HPO alignment", "limitations": "Limitations",
      "about": "About", "researchEthics": "Research ethics", "careers": "Careers", "contact": "Contact",
      "privacy": "Privacy", "terms": "Terms", "dpa": "DPA", "responsibleUse": "Responsible use"
    }
  }
}
```

- [ ] **Step 2: `src/i18n/locales/ru/marketing.json`** — same key tree, Russian values (proper nouns from the glossary stay verbatim):

```json
{
  "nav": {
    "product": "Продукт",
    "evidence": "Доказательства",
    "forClinicians": "Для врачей",
    "research": "Исследования"
  },
  "story": {
    "act1Eyebrow": "Вычислительная дисморфология",
    "act1Tag": "PG · 01",
    "title": "Фенотип, прочитанный точно.",
    "lede": "Phenograph определяет морфологические признаки по одному изображению лица и ранжирует возможные синдромы по онтологии HPO.",
    "ctaPrimary": "Запросить клинический доступ",
    "ctaSecondary": "Изучить доказательства",
    "figIdle": "РИС. 01 — ИСХОДНОЕ СОСТОЯНИЕ",
    "figLine1": "Двухцепочечный эталонный паттерн.",
    "figLine2": "127 морфологических признаков индексировано.",
    "figLine3": "4 317 синдромов связаны с HPO.",
    "scrollToContinue": "Прокрутите, чтобы продолжить",
    "scroll": "Прокрутка",
    "act2Tag": "PG · 02 / СЕТКА",
    "act2Title": "От цепочки кода к лицу в эталоне.",
    "act2Body": "Субмиллиметровые ориентиры локализуются по тщательно подобранному атласу из 127 морфологических признаков, затем триангулируются в эталонную сетку для метрического анализа.",
    "meshStage": "ЭТАП",
    "meshLandmarks": "Ориентиры",
    "meshEdges": "Рёбра",
    "meshAtlas": "Атлас",
    "meshModel": "Модель",
    "meshDisclaimer": "Все изображения — абстрактная эталонная сетка. Phenograph никогда не раскрывает узнаваемые лица пациентов.",
    "stages": {
      "arrival": "01 · ПОСТУПЛЕНИЕ",
      "landmarking": "02 · РАЗМЕТКА",
      "triangulation": "03 · ТРИАНГУЛЯЦИЯ",
      "contour": "04 · КОНТУР",
      "annotated": "05 · АННОТАЦИЯ"
    },
    "act3Tag": "PG · 03 / ЗАПИСЬ",
    "act3Title": "Подтверждённые доказательства, тихо накапливающиеся.",
    "asOf": "На апрель 2026",
    "liveDataset": "● ЖИВОЙ НАБОР ДАННЫХ",
    "disclaimerShort": "Не является диагностическим устройством. Только поддержка клинических решений.",
    "publications": "Публикации",
    "validationReport": "Отчёт о валидации",
    "stat": {
      "accuracyLabel": "Точность Top-5 на слепой валидации",
      "accuracySub": "n = 12 400 генетически подтверждённых случаев",
      "accuracyChart": "2018 — 2025",
      "syndromesLabel": "Синдромов связано в графе знаний",
      "syndromesSub": "Согласовано с HPO · обновляется ежеквартально",
      "syndromesChart": "6 релизов",
      "featuresLabel": "Морфологических признаков оценено",
      "featuresSub": "Каждый с калиброванной неопределённостью",
      "featuresChart": "Рост атласа",
      "pubsLabel": "Рецензируемых публикаций",
      "pubsSub": "Nature Genetics · AJHG · JAMA Pediatrics",
      "pubsChart": "по годам"
    },
    "rail": { "read": "Чтение", "mesh": "Сетка", "record": "Запись" }
  },
  "features": {
    "eyebrow": "Что он делает",
    "heading": "Тихий инструмент, расширяющий клиническую экспертизу, а не заменяющий её.",
    "landmark": { "title": "Фенотипирование по ориентирам", "body": "Субмиллиметровая разметка лица по тщательно подобранному атласу из 127 морфологических признаков с оценкой неопределённости." },
    "ranking": { "title": "Ранжирование синдромов", "body": "Байесовское ранжирование более чем 4 300 синдромов, связанных с Human Phenotype Ontology. Врачи видят апостериорные вероятности, а не оценки «чёрного ящика»." },
    "workflow": { "title": "Клинический процесс", "body": "Заметки по случаю, признаки и панели кандидатов экспортируются в FHIR. Интегрируется с существующими ЭМК без перестройки." }
  },
  "evidence": {
    "eyebrow": "Доказательства",
    "heading": "Проверено на более чем 12 000 генетически подтверждённых случаев в 41 учреждении.",
    "accuracy": "Точность Top-5 на слепом валидационном наборе случаев редких синдромов.",
    "syndromes": "Синдромов охвачено в связанном графе знаний. Согласовано с HPO.",
    "publications": "Рецензируемых публикаций. Nature Genetics, AJHG, JAMA Pediatrics."
  },
  "footer": {
    "tagline": "Вычислительная дисморфология для клинической генетики.",
    "copyright": "© 2026 Phenograph, Inc. Не является диагностическим устройством. Только для поддержки клинических решений.",
    "cols": {
      "product": "Продукт",
      "evidence": "Доказательства",
      "company": "Компания",
      "legal": "Юридическое"
    },
    "links": {
      "overview": "Обзор", "clinicalWorkflow": "Клинический процесс", "integrations": "Интеграции", "security": "Безопасность", "changelog": "Изменения",
      "publications": "Публикации", "validation": "Валидация", "hpoAlignment": "Согласование с HPO", "limitations": "Ограничения",
      "about": "О нас", "researchEthics": "Этика исследований", "careers": "Карьера", "contact": "Контакты",
      "privacy": "Конфиденциальность", "terms": "Условия", "dpa": "DPA", "responsibleUse": "Ответственное использование"
    }
  }
}
```

- [ ] **Step 3: Register the namespace in `resources.ts`**

Add imports and entries (keep alphabetical-ish, matching pattern):

```ts
import enMarketing from './locales/en/marketing.json';
import ruMarketing from './locales/ru/marketing.json';
```

Add `'marketing'` to `NAMESPACES`, and `marketing: enMarketing` / `marketing: ruMarketing` to the `en` / `ru` objects in `resources`.

- [ ] **Step 4: Refactor `sections.tsx`**

Replace hardcoded strings with `useTranslation('marketing')`. Concretely:
- `MarketingNav`: `const { t } = useTranslation('marketing');` Replace the `['Product','Evidence','For clinicians','Research']` array with keyed items: `[['product','nav.product'],['evidence','nav.evidence'],['forClinicians','nav.forClinicians'],['research','nav.research']].map(([k, key]) => <a key={k} ...>{t(key)}</a>)`. Replace `Sign in` → `{tc('actions.signIn')}` and `Request access` → `{tc('actions.requestAccess')}` where `const { t: tc } = useTranslation('common');`. **Add `<LanguageSwitcher />`** (import from `../../components/ui`) into the right-hand `<div>` before the Sign in link.
- `FeatureGrid`: build `features` from keys: `[{ icon: ScanFace, k: 'landmark' }, { icon: Dna, k: 'ranking' }, { icon: FileSearch, k: 'workflow' }]`, rendering `t(\`features.${k}.title\`)` / `t(\`features.${k}.body\`)`. Eyebrow → `t('features.eyebrow')`, heading → `t('features.heading')`. Use a stable `key={k}`.
- `EvidenceBand`: stats become `[['92%','evidence.accuracy'],['4,317','evidence.syndromes'],['17','evidence.publications']]` → `t(key)`. Eyebrow → `t('evidence.eyebrow')`, heading → `t('evidence.heading')`.
- `MarketingFooter`: columns keyed: `[{ k:'product', links:['overview','clinicalWorkflow','integrations','security','changelog'] }, { k:'evidence', links:['publications','validation','hpoAlignment','limitations'] }, { k:'company', links:['about','researchEthics','careers','contact'] }, { k:'legal', links:['privacy','terms','dpa','responsibleUse'] }]`; title → `t(\`footer.cols.${col.k}\`)`, link → `t(\`footer.links.${l}\`)`. Tagline → `t('footer.tagline')`, copyright → `t('footer.copyright')`. Leave `SOC 2 · HIPAA · GDPR` literal.

- [ ] **Step 5: Refactor `ScrollStory.tsx`**

Add `const { t } = useTranslation('marketing');` inside each act component (`ActTitle`, `ActMesh`, `ActStats`, `ProgressRail`). Map every literal to its `story.*` key. Specifically:
- `ActTitle`: eyebrow `Computational dysmorphology` → `t('story.act1Eyebrow')` (keep the `PG · 01` / `/` mono prefix literal); title → `t('story.title')`; lede → `t('story.lede')`; CTAs → `t('story.ctaPrimary')`, `t('story.ctaSecondary')`; fig block → `t('story.figIdle')`, `figLine1..3`, `t('story.scrollToContinue')`; scroll hint → `t('story.scroll')`. Leave `HIPAA-aligned / SOC 2 Type II / GDPR` literal.
- `ActMesh`: `stage` ternary returns `t('story.stages.arrival')` etc.; tag → `t('story.act2Tag')`; title/body → `t('story.act2Title')`/`act2Body`; the metric rows `k` labels → `t('story.meshLandmarks'|'meshEdges'|'meshAtlas'|'meshModel')` (keep values `pg-face-3.2`, `HPO v2024.4`, counts literal); `STAGE` → `t('story.meshStage')`; disclaimer → `t('story.meshDisclaimer')`.
- `ActStats`: stat `label`/`sub`/`chartLabel` → `t('story.stat.*')` per the keys above (numeric `value`/`suffix` stay computed); tag → `t('story.act3Tag')`; title → `t('story.act3Title')`; `As of Apr 2026` → `t('story.asOf')`; `● LIVE DATASET` → `t('story.liveDataset')`; bottom disclaimer → `t('story.disclaimerShort')`; links → `t('story.publications')`, `t('story.validationReport')`.
- `ProgressRail`: `items` become `[['01','rail.read'],['02','rail.mesh'],['03','rail.record']]` → `t(\`story.${key}\`)`.

- [ ] **Step 6: Run parity tests + build**

Run: `npm test -- i18n` then `npx tsc -b`
Expected: parity PASS for `marketing`; tsc clean.

- [ ] **Step 7: Eyeball both languages**

Run: `npm run dev`; open landing; toggle the switcher RU↔EN; reload and confirm choice persisted. Stop server.

- [ ] **Step 8: Commit**

```bash
git add src/i18n/locales src/i18n/resources.ts src/features/marketing
git commit -m "feat(i18n): localize marketing landing + add language switcher"
```

---

## Task 11: Translate auth (layout, login, register)

**Files:** Create `src/i18n/locales/{ru,en}/auth.json`; Modify `src/i18n/resources.ts`, `src/features/auth/{AuthLayout,LoginPage,RegisterPage}.tsx`

- [ ] **Step 1: `src/i18n/locales/en/auth.json`**

```json
{
  "login": {
    "title": "Sign in",
    "subtitle": "Access your clinical workspace.",
    "footer": "Need an account?",
    "error": "Unable to sign in. Please try again."
  },
  "register": {
    "title": "Request access",
    "subtitle": "Create an account for clinical decision support.",
    "footer": "Already have an account?",
    "error": "Unable to create the account. Please try again.",
    "passwordTooShort": "Password must be at least 8 characters."
  },
  "fields": {
    "email": "Email",
    "password": "Password",
    "fullName": "Full name",
    "accountType": "Account type",
    "accountTypeHint": "Doctors review individual cases; labs submit batches.",
    "organization": "Organization",
    "organizationHint": "Optional.",
    "passwordHint": "At least 8 characters.",
    "emailPlaceholder": "you@hospital.org",
    "namePlaceholder": "Dr. Jane Doe",
    "orgPlaceholder": "Boston Children's Hospital",
    "roleDoctor": "Clinician (doctor)",
    "roleLab": "Laboratory"
  }
}
```

- [ ] **Step 2: `src/i18n/locales/ru/auth.json`**

```json
{
  "login": {
    "title": "Войти",
    "subtitle": "Доступ к вашему клиническому рабочему пространству.",
    "footer": "Нет аккаунта?",
    "error": "Не удалось войти. Пожалуйста, попробуйте снова."
  },
  "register": {
    "title": "Запросить доступ",
    "subtitle": "Создайте аккаунт для поддержки клинических решений.",
    "footer": "Уже есть аккаунт?",
    "error": "Не удалось создать аккаунт. Пожалуйста, попробуйте снова.",
    "passwordTooShort": "Пароль должен содержать не менее 8 символов."
  },
  "fields": {
    "email": "Email",
    "password": "Пароль",
    "fullName": "Полное имя",
    "accountType": "Тип аккаунта",
    "accountTypeHint": "Врачи разбирают отдельные случаи; лаборатории отправляют пакеты.",
    "organization": "Организация",
    "organizationHint": "Необязательно.",
    "passwordHint": "Не менее 8 символов.",
    "emailPlaceholder": "you@hospital.org",
    "namePlaceholder": "Иванов Иван Иванович",
    "orgPlaceholder": "Детская больница",
    "roleDoctor": "Врач (клиницист)",
    "roleLab": "Лаборатория"
  }
}
```

- [ ] **Step 3: Register namespace** in `resources.ts` (add `auth` imports, push `'auth'` to `NAMESPACES`, add to both locale objects — same pattern as Task 10 Step 3).

- [ ] **Step 4: Refactor the three components**

- `AuthLayout.tsx`: no literal copy to change (title/subtitle/footer are passed in) — leave as-is.
- `LoginPage.tsx`: `const { t } = useTranslation('auth');` Title `"Sign in"` → `t('login.title')`; subtitle → `t('login.subtitle')`; footer → `<>{t('login.footer')} <Link to="/register">{tc('actions.requestAccess')}</Link></>` with `const { t: tc } = useTranslation('common');`. Field labels → `t('fields.email')`/`t('fields.password')`; placeholder → `t('fields.emailPlaceholder')`. Error fallback string → `t('login.error')`. Submit button → `t('login.title')`.
- `RegisterPage.tsx`: title → `t('register.title')`; subtitle → `t('register.subtitle')`; footer → `<>{t('register.footer')} <Link to="/login">{tc('actions.signIn')}</Link></>`. Field labels/hints/placeholders → `t('fields.*')`; the two `<option>`s → `t('fields.roleDoctor')`/`t('fields.roleLab')`. `passwordTooShort` and `error` fallbacks → `t('register.passwordTooShort')`/`t('register.error')`. Submit button → `tc('actions.createAccount')`.

- [ ] **Step 5: Tests + build**

Run: `npm test -- i18n` then `npx tsc -b`. Expected: parity PASS for `auth`, tsc clean.

- [ ] **Step 6: Commit**

```bash
git add src/i18n src/features/auth
git commit -m "feat(i18n): localize auth pages"
```

---

## Task 12: Translate shared UI defaults

**Files:** Modify `src/components/ui/{feedback,Pagination,Dropzone}.tsx`

These use the `common` namespace created in Task 4 — no new JSON needed.

- [ ] **Step 1: `feedback.tsx`**

- `StatusPill`: the `statusStyle` map currently embeds `label`. Remove `label` from the map values and render `t(\`status.${status}\`)` via `const { t } = useTranslation('common');` (status keys already exist: pending/processing/completed/failed). Keep colors/pulse in the map.
- `FullPageSpinner`: default `label = 'Loading…'` → make `label` optional and fall back to `t('state.loading')` inside the component (it needs the hook now). Since `FullPageSpinner` is rendered before the tree in `App` loading state, calling `useTranslation` is fine (i18n is initialized at import time).
- `ErrorState`: default `title = 'Something went wrong'` → resolve from `t('state.somethingWentWrong')` when no `title` passed.

  Example for `ErrorState`:

  ```tsx
  export function ErrorState({ title, message, action }: { title?: string; message?: string; action?: ReactNode }) {
    const { t } = useTranslation('common');
    return <StateBlock icon={<TriangleAlert size={28} strokeWidth={1.5} color="var(--danger)" />} title={title ?? t('state.somethingWentWrong')} message={message} action={action} />;
  }
  ```

- [ ] **Step 2: `Pagination.tsx`**

`const { t } = useTranslation('common');` `Page {page}` → `t('pagination.page', { page })`; `Prev` → `t('pagination.prev')`; `Next` → `t('pagination.next')`.

- [ ] **Step 3: `Dropzone.tsx`**

`const { t } = useTranslation('common');` Replace `Drop {multiple ? 'images' : 'an image'} here, or click to browse` with `multiple ? t('dropzone.promptMany') : t('dropzone.promptOne')`.

- [ ] **Step 4: Build + tests**

Run: `npx tsc -b` then `npm test`. Expected: clean / green.

- [ ] **Step 5: Commit**

```bash
git add src/components/ui
git commit -m "feat(i18n): localize shared UI defaults (status, states, pagination, dropzone)"
```

---

## Task 13: Translate app shell, sidebar + nav

**Files:** Modify `src/components/layout/nav.ts`, `src/components/layout/Sidebar.tsx`, `src/components/layout/AppShell.tsx`

- [ ] **Step 1: Add stable keys to `nav.ts`**

Add `key: string` to the `NavItem` interface. Set each item's `key` to its `nav.json` key and change `label` to that same key string (label is now an i18n key resolved by the consumer). Doctor items: `cases→'cases'`, `New analysis→'newAnalysis'`, `Syndromes→'syndromes'`, `Feature atlas→'atlas'`, `Notifications→'notifications'`, `Profile→'profile'`. Lab items: `Batches→'batches'`, `New batch→'newBatch'`, `Usage→'usage'`, `Billing→'billing'`, plus `syndromes`, `notifications`, `profile`. Concretely:

```ts
export interface NavItem {
  to: string;
  key: string;       // i18n key in the `nav` namespace
  icon: LucideIcon;
  end?: boolean;
}

const doctorNav: NavItem[] = [
  { to: '/app/cases', key: 'cases', icon: Folder, end: true },
  { to: '/app/cases/new', key: 'newAnalysis', icon: ScanFace },
  { to: '/app/syndromes', key: 'syndromes', icon: Dna },
  { to: '/app/atlas', key: 'atlas', icon: Microscope },
  { to: '/app/notifications', key: 'notifications', icon: Bell },
  { to: '/app/profile', key: 'profile', icon: User },
];

const labNav: NavItem[] = [
  { to: '/app/batches', key: 'batches', icon: Layers, end: true },
  { to: '/app/batches/new', key: 'newBatch', icon: Upload },
  { to: '/app/usage', key: 'usage', icon: Gauge },
  { to: '/app/billing', key: 'billing', icon: CreditCard },
  { to: '/app/syndromes', key: 'syndromes', icon: Dna },
  { to: '/app/notifications', key: 'notifications', icon: Bell },
  { to: '/app/profile', key: 'profile', icon: User },
];
```

- [ ] **Step 2: Update `Sidebar.tsx`**

`const { t } = useTranslation('nav');` Render `t(item.key)` for the label. Replace the brittle badge check `item.label === 'Notifications'` with `item.key === 'notifications'`. Add the switcher to the footer block (above or below the user/org lines): `import { LanguageSwitcher } from '../ui';` then `<div style={{ marginBottom: 10 }}><LanguageSwitcher compact /></div>` inside the `marginTop: 'auto'` footer div.

- [ ] **Step 3: Update `AppShell.tsx`**

`const { t } = useTranslation('common');` (+ a `nav` instance for button labels). `primary` becomes `{ labelKey: 'newBatch'|'newCase', to }` — but "New case" isn't a nav key, so use literal keys: lab → `{ label: tn('newBatch'), to: '/app/batches/new' }`, doctor → `{ label: t('... new case')` }. Add a `common` key for "New case": add `"newCase": "New case"` / `"Новый случай"` to `common.json` under a new `"appbar"` group, e.g. `appbar.newCase`. Then primary label = `user?.role === 'lab' ? tn('newBatch') : t('appbar.newCase')`. Replace `aria-label="Open navigation"` → `t('appbar.openNav')`, `aria-label="Notifications"` → `tn('notifications')` (nav ns), `title="Sign out"` → `t('actions.signOut')`. Add the corresponding keys (`appbar.newCase`, `appbar.openNav`) to **both** `common.json` files.

  Add to `common.json` (en/ru):
  - en: `"appbar": { "newCase": "New case", "openNav": "Open navigation" }`
  - ru: `"appbar": { "newCase": "Новый случай", "openNav": "Открыть навигацию" }`

- [ ] **Step 4: Tests + build**

Run: `npm test -- i18n` (parity must still pass after the `common` additions) then `npx tsc -b`.

- [ ] **Step 5: Commit**

```bash
git add src/components/layout src/i18n/locales
git commit -m "feat(i18n): localize app shell, sidebar nav + in-app switcher"
```

---

## Task 14: Translate cases pages

**Files:** Create `src/i18n/locales/{ru,en}/cases.json`; Modify `src/i18n/resources.ts`, `src/features/cases/{CasesListPage,NewCasePage,CaseDetailPage}.tsx`

- [ ] **Step 1: Extract strings**

Read each of the three files. For every user-visible literal (page eyebrow/title, table headers, button labels, empty/error/loading messages, form labels/hints/placeholders, status/section headings, toast messages, confirmation prompts), add a key under a sensible sub-tree in `cases.json` (`list.*`, `new.*`, `detail.*`, plus shared `cases.*`). Keep numeric/interpolated values as `{{var}}` placeholders (e.g. case counts, confidence percentages, dates already go through `formatDate`). Author the EN file with the **verbatim** current English and the RU file from the glossary.

- [ ] **Step 2: Register namespace** in `resources.ts` (`cases` imports, add to `NAMESPACES` + both locale objects).

- [ ] **Step 3: Refactor the three components** to `useTranslation('cases')` (and `common` for shared actions/states). Replace each literal with its `t(key)`. Use `t(key, { count })` with `_one/_few/_many` plural keys wherever a Russian count+noun is rendered (e.g. "{{count}} cases"). Reuse `common` status keys for `StatusPill` (already handled in Task 12) and `common.state.*` for loading/error/empty where the page passes custom copy.

- [ ] **Step 4: Parity + build + smoke**

Run: `npm test -- i18n` then `npx tsc -b` then `npm run dev` and visit Cases (mock login `doctor@phenograph.test` / `password`), toggling languages. Expected: parity PASS, tsc clean, no English leaks on the cases screens.

- [ ] **Step 5: Commit**

```bash
git add src/i18n src/features/cases
git commit -m "feat(i18n): localize cases pages"
```

---

## Task 15: Translate labs pages

**Files:** Create `src/i18n/locales/{ru,en}/labs.json`; Modify `src/i18n/resources.ts`, `src/features/labs/{BatchesPage,NewBatchPage,BatchDetailPage,UsagePage,BillingPage}.tsx`

- [ ] **Step 1–5:** Identical procedure to Task 14, for the five labs pages. Sub-trees: `batches.*`, `newBatch.*`, `batchDetail.*`, `usage.*`, `billing.*`. Watch for: usage metrics/quota labels, billing plan/invoice copy, batch status counts (use plural keys), upload hints (Dropzone already localized — pass `hint` via `t`). Register `labs` in `resources.ts`. Verify with mock login `lab@phenograph.test` / `password`.

- [ ] **Commit**

```bash
git add src/i18n src/features/labs
git commit -m "feat(i18n): localize labs pages"
```

---

## Task 16: Translate reference pages (syndromes, atlas)

**Files:** Create `src/i18n/locales/{ru,en}/reference.json`; Modify `src/i18n/resources.ts`, `src/features/reference/{SyndromesPage,SyndromeDetailPage,HpoAtlasPage}.tsx`

- [ ] **Step 1–5:** Same procedure. Sub-trees: `syndromes.*`, `syndromeDetail.*`, `atlas.*`. Keep medical proper nouns / HPO IDs / syndrome names that come from API data **untranslated** (only translate static UI chrome: headings, column labels, search placeholders, empty/error states, filter labels). Register `reference` in `resources.ts`.

- [ ] **Commit**

```bash
git add src/i18n src/features/reference
git commit -m "feat(i18n): localize reference (syndromes + atlas) pages"
```

---

## Task 17: Translate notifications, profile, not-found

**Files:** Create `src/i18n/locales/{ru,en}/notifications.json`, `src/i18n/locales/{ru,en}/profile.json`; Modify `src/i18n/resources.ts`, `src/features/notifications/NotificationsPage.tsx`, `src/features/profile/ProfilePage.tsx`, `src/features/NotFoundPage.tsx`

- [ ] **Step 1: `notifications.json`** (en / ru)

en:
```json
{
  "eyebrow": "Workspace",
  "title": "Notifications",
  "loadError": "Could not load notifications.",
  "emptyTitle": "No notifications",
  "emptyMessage": "You're all caught up."
}
```
ru:
```json
{
  "eyebrow": "Рабочее пространство",
  "title": "Уведомления",
  "loadError": "Не удалось загрузить уведомления.",
  "emptyTitle": "Нет уведомлений",
  "emptyMessage": "Вы всё просмотрели."
}
```

- [ ] **Step 2: `profile.json`** (en / ru)

en:
```json
{
  "eyebrow": "Account",
  "title": "Profile",
  "name": "Name",
  "email": "Email",
  "accountType": "Account type",
  "organization": "Organization",
  "managedNote": "Profile details are managed by your organization administrator.",
  "roles": { "doctor": "Clinician", "lab": "Laboratory", "admin": "Administrator" }
}
```
ru:
```json
{
  "eyebrow": "Аккаунт",
  "title": "Профиль",
  "name": "Имя",
  "email": "Email",
  "accountType": "Тип аккаунта",
  "organization": "Организация",
  "managedNote": "Данные профиля управляются администратором вашей организации.",
  "roles": { "doctor": "Врач", "lab": "Лаборатория", "admin": "Администратор" }
}
```

- [ ] **Step 3: Add NotFound keys to `common.json`** (both locales), e.g. under `notFound`:
  - en: `"notFound": { "title": "Page not found", "message": "The page you were looking for doesn't exist or has moved." }`
  - ru: `"notFound": { "title": "Страница не найдена", "message": "Страница, которую вы искали, не существует или была перемещена." }`

- [ ] **Step 4: Register `notifications` + `profile`** in `resources.ts` (imports + `NAMESPACES` + both locale objects).

- [ ] **Step 5: Refactor components**

- `NotificationsPage.tsx`: `useTranslation('notifications')`; eyebrow/title → keys; `ErrorState message` → `t('loadError')`; `EmptyState` title/message → `t('emptyTitle')`/`t('emptyMessage')`; `Spinner label="Loading…"` → `tc('state.loading')`; `Retry` button → `tc('actions.retry')`; `title="Mark as read"` → `tc('actions.markAsRead')`.
- `ProfilePage.tsx`: `useTranslation('profile')`; replace `roleLabel` map with `t(\`roles.${user.role}\`)`; rows labels → `t('name')`/`t('email')`/`t('accountType')`/`t('organization')`; organization empty `'—'` → `tc('value.none')`; eyebrow/title → keys; managed note → `t('managedNote')`; `Sign out` → `tc('actions.signOut')`.
- `NotFoundPage.tsx`: `useTranslation('common')`; `Page not found` → `t('notFound.title')`; message → `t('notFound.message')`; `Back to home` → `t('actions.backToHome')`. Keep `404` literal.

- [ ] **Step 6: Parity + build**

Run: `npm test -- i18n` then `npx tsc -b`. Expected: parity PASS for all namespaces, tsc clean.

- [ ] **Step 7: Commit**

```bash
git add src/i18n src/features/notifications src/features/profile src/features/NotFoundPage.tsx
git commit -m "feat(i18n): localize notifications, profile, not-found"
```

---

## Task 18: Bilingual smoke render test

**Files:** Create `src/features/marketing/LandingPage.test.tsx`

- [ ] **Step 1: Write the test**

`src/features/marketing/LandingPage.test.tsx`:

```tsx
import { describe, expect, it, beforeEach } from 'vitest';
import { render, screen } from '@testing-library/react';
import { MemoryRouter } from 'react-router-dom';
import i18n from '../../i18n';
import { LandingPage } from './LandingPage';

function renderLanding() {
  return render(<MemoryRouter><LandingPage /></MemoryRouter>);
}

describe('LandingPage i18n', () => {
  beforeEach(async () => { await i18n.changeLanguage('ru'); });

  it('renders Russian hero copy by default', () => {
    renderLanding();
    expect(screen.getByText(/Фенотип, прочитанный точно\./i)).toBeInTheDocument();
  });

  it('renders English hero copy after switching', async () => {
    await i18n.changeLanguage('en');
    renderLanding();
    expect(screen.getByText(/Phenotype, precisely read\./i)).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Run**

Run: `npm test -- LandingPage`
Expected: PASS both. (If the scroll-story components rely on `IntersectionObserver` and jsdom lacks it, add a minimal stub to `src/test/setup.ts`: `globalThis.IntersectionObserver ??= class { observe(){} unobserve(){} disconnect(){} } as any;` and re-run.)

- [ ] **Step 3: Commit**

```bash
git add src/features/marketing/LandingPage.test.tsx src/test/setup.ts
git commit -m "test(i18n): bilingual smoke render of landing page"
```

---

## Task 19: Final verification

- [ ] **Step 1: Full test suite**

Run: `npm test`
Expected: all suites green (config, parity for all 9 namespaces, switcher, landing smoke).

- [ ] **Step 2: Type-check + production build**

Run: `npm run build`
Expected: `tsc -b` clean, `vite build` succeeds.

- [ ] **Step 3: Lint**

Run: `npm run lint`
Expected: clean (fix any unused-import / hook-rule violations introduced).

- [ ] **Step 4: Manual walkthrough**

Run: `npm run dev`. With `VITE_USE_MOCKS=true`, log in as both `doctor@phenograph.test` and `lab@phenograph.test` (`password`). Click through every nav destination in **Russian** (default), confirm no English leaks; switch to English via the sidebar switcher and re-check a couple of pages; reload and confirm the language persisted. Stop the server.

- [ ] **Step 5: Update README**

Add a short "Localization" section to `frontend/app/README.md`: default language is Russian, switch via the marketing nav or sidebar, choice persists in `localStorage` (`phenograph.lang`), translations live in `src/i18n/locales/<lng>/<namespace>.json`, and the key-parity test enforces RU/EN completeness.

- [ ] **Step 6: Commit**

```bash
git add frontend/app/README.md
git commit -m "docs: document localization in frontend README"
```

---

## Self-Review notes (verified against the spec)

- **Library / scope / persistence / switcher placement / proper nouns** — all spec decisions map to Tasks 1, 5 (detection+fallback), 8/10/13 (switcher in marketing + sidebar), and the glossary's verbatim list.
- **Whole-app coverage** — namespaces cover every feature folder: marketing (10), auth (11), shared UI (12), shell/nav (13), cases (14), labs (15), reference (16), notifications/profile/not-found (17).
- **Testing** — key-parity (6, dynamic over `NAMESPACES`), config default+persistence (6), switcher (8), bilingual smoke (18); build/lint/manual in 19.
- **Type safety** — Task 7 augmentation; `tsc -b` gated after every namespace task.
- **No placeholders** — infra + marketing/auth/notifications/profile/common/nav JSON given verbatim; cases/labs/reference (Tasks 14–16) are mechanical extraction tasks with an explicit procedure, sub-tree naming, glossary, and per-page verification, since reproducing ~900 lines of source strings inline would duplicate the implementation. Each still ends at green parity + clean build.
