# Base URL of the Oblivion API gateway (nginx routes by path prefix).
# Replace with the real domain in production.
VITE_API_BASE_URL=http://localhost:8080

# When "true", the app runs against in-browser MSW mocks instead of the real
# backend — useful for demos and local UI work without a running gateway.
VITE_USE_MOCKS=false
